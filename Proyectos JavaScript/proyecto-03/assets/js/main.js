let actualSpeed = 60;
const  limitSpeed = 60;

if(actualSpeed > limitSpeed){

    console.log('Reduzca la velocidad. Vas a ' + actualSpeed + 'km/h y el límite es de '+limitSpeed+'km/h.');
}
else{
    console.log('Todo va bien');
}
